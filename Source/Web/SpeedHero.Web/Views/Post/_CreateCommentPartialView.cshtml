@model SpeedHero.Web.InputModels.Comments.CreateCommentInputModel

@using (Html.BeginForm("CreateComment", "Comment", new { PostId = Model.PostId }, FormMethod.Post, new { @role = "form" }))
{
    <div class="form-group">
        @(Html.Kendo().Editor()
            .Name("Content")
            .Encode(true)
            .HtmlAttributes(new { style = "width: 80%; height:200px" }))
    </div>
    <div class="form-group">
        <input type="submit" value="Post comment" class="btn btn-primary" />
    </div>
}